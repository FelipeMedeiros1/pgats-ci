# CI de Nível 01 - Disparo automático por agendamento
#teste2e - Playwright
# nome da pipeline
name: 'Execução Agendada'

# gatilhos / triggers - regras de disparo
# gatilho automático por agendamento = schedule

on:
  schedule:
    # Agendamento para rodar as 13h todos os dias úteis (segunda a sexta) no horário de São Paulo
    # 1- miunuto (0-59)
    # 2- hora (0-23)
    # 3- dia do mês (1-31)
    # 4- mês (1-12)
    # 5- dia da semana (0-7) (0 ou 7 = domingo)
    # Timezone: America/Sao_Paulo (UTC-3)

    - cron: '0 16 * * *' # 16:00 UTC = 13:00 BRT (horário de verão) / 14:00 BRT (horário padrão)
      timezone: 'America/Sao_Paulo'

#tarefas dentro da pipeline = jobs

jobs:
  #nome do job
  e2e-tests:
    # maquina onde o job vai rodar
    runs-on: ubuntu-latest

    # passos do job
    steps:
      # clone do projeto
      - uses: actions/checkout@v4

      # instalção do node
      - uses: actions/setup-node@v4
        with:
          node-version: 24.x

      # instalação do Yarn
      - name: Instalando Yarn
        run: npm install -g yarn

      #   instalação das dependências
      - name: Instalando dependências
        run: yarn

      # instalação dos Browsers do Playwright
      - name: Instalando Browsers do Playwright
        run: yarn playwright install

      # executar os testes e2e (gera results.json, results.xml e playwright-report/)
      - name: Executando testes e2e
        run: yarn run e2e

      # -------------------------------------------------------
      # Relatório de testes via Marketplace:
      # daun/playwright-report-summary@v4
      # https://github.com/marketplace/actions/playwright-report-comment
      # Lê o results.json do Playwright e publica:
      #   • sumário na aba "Summary" da execução (job-summary)
      #   • comentário automático em Pull Requests (create-comment)
      # -------------------------------------------------------
      - name: Publicando resumo dos testes e2e
        uses: daun/playwright-report-summary@v4
        if: always()
        with:
          report-file: results.json
          job-summary: true
          comment-title: 'Resultados dos Testes E2E'
          icon-style: emojis
          sections: 'failed, flaky, skipped, passed'

      # upload do relatório HTML como artefato para download
      - name: Upload do relatório HTML
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 7
